# どのまな Typography Token運用仕様 v3.0

- 版: v3.0（改訂2・レビュー待ち・未承認・未統合）
- 発行: 2026年7月（初版）／改訂1: 2026年7月（Phase17.5-D）／改訂2: 2026年7月（Phase17.10-B）
- 位置づけ: `donomana-design-system-v2_0.html`（共通デザインシステム Ver.2.1）**1.3節「タイポグラフィ」を正本とする補足・実装ブリッジ文書**。1.3節の内容を置き換えるものではない（詳細は0節）。
- 根拠調査: Phase17.5-A「Typography Design Investigation」（全29アプリ・generate.js・既存文書の読み取り調査、コード変更なし）
- 初版起草: Phase17.5-B／レビュー: Phase17.5-C／改訂1: Phase17.5-D／横展開実証: Phase17.7-C〜17.9-C／総括レビュー: Phase17.10-A／改訂2（本節群の追記）: Phase17.10-B（いずれも文書化・レビューのみ、コード変更なし）
- 参照元: `donomana-design-system-v2_0.html` 9.9.1 稼働中コードとの衝突ログ #7

> **本文書はドラフトである。** ここに記載する数値・方針は次Phase以降の承認を経るまで正式仕様として確定しない。generate.js・アプリHTML・既存Token値/Token名は本文書のみを理由に変更してはならない。

---

## 0. 本文書の背景と位置づけ（Phase17.5-D改訂）

**訂正**: 初版（Phase17.5-B）は「Design Systemに正式なTypography章が存在しない」と記載したが、これは誤りだった。Phase17.5-Cのレビューで、`donomana-design-system-v2_0.html`の**1.3節「タイポグラフィ」に、印刷実寸(mm)・印刷pt・Web pxを対応させた正式なタイプスケールが既に存在する**ことが判明した。存在しないのは「1.3節のようなTypography仕様」ではなく、**「Design System全体をVer.3.0として統合した1つの正式文書」**である。この2つを混同しない。

整理すると次のとおりである。

| 項目 | 状態 |
|---|---|
| Typographyの正式な数値仕様（1.3節） | **存在する**（`donomana-design-system-v2_0.html` 1.3節、内部版Ver.2.1） |
| Design System全体のVer.3.0文書 | **存在しない**（現行の正本はVer.2.1のまま。generate.jsのコメントが「Design System 3.0」と呼んでいるのは、Token実装の呼称であり文書のバージョンではない） |
| Web実装向けのToken運用ルール（本文書） | 存在しない（今回新規に起草） |

本文書の役割は、1.3節が定めた**印刷物込みのタイプスケール**を、**Web実装（CSS変数・rem単位・ブラウザ/OSの文字サイズ設定への追随）に落とし込むための運用仕様**を補うことである。1.3節の数値・方針を上書き・変更するものではない。1.3節と本文書の間に矛盾が見つかった場合は、1.3節が正本であり、本文書側を修正する。

Phase17.5-Aの全29アプリ実地調査（font-size 1,974件・line-height 245件・font-family 37種類）は、「1.3節の仕様」と「実装されている29アプリの現状」がどれだけ一致しているかを検証するために行った。結果は4節・5節・9節に記載する。

---

## 1. 基本原則

1. **Typographyは意味ベースで指定する。** 「見出しである」「本文である」「補助文である」という役割に基づいてTokenを選び、数値の見た目の近さだけで判断しない。
2. **単なる数値一致ではTokenへ置換しない。** ある値がToken値と偶然一致していても、用途（意味）が一致しない限り置換禁止とする（詳細は5節・10節）。
3. **ブラウザやOSの文字サイズ設定への追随性を優先する。** ユーザーが自分の見やすい文字サイズをブラウザ／OSで設定した場合、教材アプリ側もそれに追随することをアクセシビリティ上の最優先事項とする。
4. **ページ全体のズーム（Ctrl/⌘ + +/-）だけを「文字拡大対応」とみなさない。** ページズームはpx指定でもrem指定でも同様に効くため、これのみを根拠に「文字拡大に対応済み」と判断してはならない。ブラウザの「文字サイズのみ変更」設定やOS側のテキストスケーリング（アクセシビリティ機能）への追随性を別に評価する。
5. **font-sizeは原則remを使用する。** pxによる固定指定は、上記4条件のいずれにも追随しないため原則禁止とする（既存コードの扱いは10節・12節）。
6. **line-heightは原則unitless（単位なし数値）を使用する。** pxや%によるline-height指定は、font-sizeを変更した際に比率が崩れる既知の問題があるため避ける。
7. **font-familyは現段階では共通Token化しない。** 理由は9節に記載する。
8. **Token値の根拠は1.3節を正本とする。** 本文書がTypography Tokenの値を示す場合、それは1.3節の数値をrem単位へ変換したものであり、独自に新しい値を作らない（0節）。

---

## 2. Typography Tokenの役割（用途の明文化）

現行Token名は変更しない。各Tokenの意味的役割と、1.3節の対応する役割名を以下のとおり対応付ける。

| Token | 想定用途 | 1.3節の対応する役割名 |
|---|---|---|
| `--dm-font-size-display` | アプリ最上位の主要な数字・文字表示（例: 見本文字の大表示、スコア等の主役級表示）。1画面に1〜数箇所のみを想定。 | 大見出し |
| `--dm-font-size-heading` | 画面・パネルの見出し（h1/h2相当、または視覚的に見出しとして機能する要素）。 | 見出し |
| `--dm-font-size-subheading` | 見出しに準ずる中見出し・セクション見出し。 | 小見出し |
| `--dm-font-size-body` | 大人（保護者・教員）向けUIの通常本文・説明文・ボタンラベル等。 | 本文（基準） |
| `--dm-font-size-body-kid` | 子ども向け画面の本文・問題文など、可読性を優先して大きめに表示する本文。 | 子ども向け本文 |
| `--dm-font-size-caption` | 注釈・補足・タイムスタンプ等、短く優先度の低い補助テキスト。 | 注記（最小） |
| `--dm-line-height-heading` | 見出し用途のみ。 | 対応する固定値の記載なし（5節参照） |
| `--dm-line-height-body` | 通常本文・説明文・モーダル本文・手順文。 | 本文（基準）の「行間1.7」 |
| `--dm-line-height-body-kid` | 子ども向けの大きな本文・問題文・読みやすさ優先の文章。 | 子ども向け本文の「行間1.8」 |
| `--dm-line-height-caption` | 注釈・補足・短い補助説明。 | 対応する固定値の記載なし（5節参照） |

**heading / body-kidの値の重複について（Phase17.5-D整理）**: `--dm-font-size-heading` と `--dm-font-size-body-kid` は現行値がどちらも24pxで同一である。1.3節を確認すると、これは偶然の一致ではなく、**別々の根拠が独立に同じ数値へ到達した結果**である。

- `見出し`＝24pxは、大見出し(32px)→見出し(24px)→小見出し(20px)というタイプスケールの1段階（Rounded 800というウェイト指定も伴う）。
- `子ども向け本文`＝24pxは、「これ以下にしない」という**アクセシビリティ上の下限**として独立に定められている。

したがって、**値が同じであることは両者を統合してよい理由にはならない**（1節の原則1・2に基づく）。この扱いは、9.8.2節が「44pxで、1.6節の60pxと別に書く」理由として述べる設計思想──**「理想の基準」と「別の目的の基準」を同じ数字で書くと、どちらの意味も守られなくなる**──と同じ考え方である。将来どちらか一方の値を変更する場合も、**Token名を分離したまま、もう一方には自動的に波及させない**運用とする。

---

## 3. font-sizeの単位方針（正式仕様案）

**案C（意味ベースのToken名を維持し、実装値はremとする）を正式仕様案として採用を提案する。**

- Token名（`--dm-font-size-body` 等）は変更しない。
- Tokenの実装値をpx固定からremへ変更する（値そのものは1.3節のpx値を単純換算する。4節参照）。
- 基準は「1rem = ユーザーまたはブラウザが設定するルート文字サイズ」であり、**16px固定を絶対の前提としない**。
- 各アプリの`html`要素・`:root`のfont-sizeは、原則としてブラウザ既定値を上書きしない。上書きしているアプリ（7節）は例外として個別管理する。

---

## 4. Token値（1.3節との対応表・rem換算）

### 4.0 既存仕様との対応表

以下は1.3節の数値をそのまま転記したものである。**推測で補完した値は含まない。** 全6役割について1.3節に印刷pt・印刷mmの記載があるため、「明記なし」に該当する項目はなかった。

| 用途 | 1.3節の名称 | 印刷mm | 印刷pt | Web px（1.3節） | Typography Token | rem換算候補（16pxルート時） | line-height（1.3節記載） |
|---|---|---|---|---|---|---|---|
| display | 大見出し | 8.5mm | 24pt | 32px | `--dm-font-size-display` | `2rem` | 記載なし |
| heading | 見出し | 6.0mm | 17pt | 24px | `--dm-font-size-heading` | `1.5rem` | 記載なし |
| subheading | 小見出し | 4.9mm | 14pt | 20px | `--dm-font-size-subheading` | `1.25rem` | 記載なし |
| body | 本文（基準） | 4.2mm | 12pt | 17px | `--dm-font-size-body` | `1.0625rem` | 1.7（`--dm-line-height-body`） |
| body-kid | 子ども向け本文 | 6.0mm | 17pt | 24px | `--dm-font-size-body-kid` | `1.5rem` | 1.8（`--dm-line-height-body-kid`、「これ以下にしない」との注記あり） |
| caption | 注記（最小） | 3.2mm | 9pt | 13px | `--dm-font-size-caption` | `0.8125rem` | 記載なし（「これ未満は禁止」との下限指定あり） |

rem換算はいずれも「1.3節のWeb px値 ÷ 16」の単純換算であり、実装慣習に合わせて値を作り変えたものではない（0節・8節の原則どおり、1.3節を正本として転記）。

### 4.1 正式方針（6Tokenすべて、1.3節の値を維持）

全6Tokenとも、**1.3節のpx値をrem単位へ単純換算した値を正式方針とする。** Phase17.5-Bの初版では、caption・heading・subheading・displayのみを「データに支持される採用候補」、bodyのみを「データが換算案を支持しない保留値」として区別していたが、Phase17.5-Cのレビューにより**この区別自体が前提を誤っていた**ことが判明したため、Phase17.5-Dで撤回する（4.2節）。

- **display = `2rem`（32px相当）**
- **heading = `1.5rem`（24px相当）**
- **subheading = `1.25rem`（20px相当）**
- **body = `1.0625rem`（17px相当）** ── 詳細は4.2節
- **body-kid = `1.5rem`（24px相当）**
- **caption = `0.8125rem`（13px相当）** ── 下限リスクについては4.5節

### 4.2 body Tokenの方針（Phase17.5-D改訂: 1remへの変更案を撤回）

**Phase17.5-B初版の誤り**: 初版は「実データで`1rem`（16px相当）が計121回、`1.0625rem`（17px相当）は計21回しかない」という実装頻度を根拠に、bodyトークンを`1rem`へ変更する案を「保留候補として提示（実質的な第一候補）」としていた。

**Phase17.5-Cのレビューで判明した誤り**: 1.3節を確認した結果、本文（基準）の17px（12pt／4.2mm）は、**印刷物とWeb表示の実寸を対応させるために意図的に選ばれた値**であり、Web開発者が慣習的に選びがちな16px刻みとは異なる根拠を持つ。したがって「実データで16px系が多い」という事実は、**Token値そのものの妥当性を否定する根拠にはならず、29アプリの実装が1.3節の仕様に元々従っていなかったことを示す実装側の乖離**と解釈すべきである。

**Phase17.5-Dの正式方針**:

- `--dm-font-size-body` は**17px相当を維持する**。rem実装時は `1.0625rem` とする。
- 「pxからremへの単位変更」と「17pxから16pxへの設計値変更」は**別の意思決定**であり、明確に分離する。本文書が扱うのは前者（単位変更）のみである。
- 17px（1.0625rem）から16px（1rem）へ変更する案自体を将来にわたって禁止するものではないが、それは**Token実装の話ではなく、1.3節（印刷実寸との対応関係を含む）そのものを見直す意思決定**であり、本文書の改訂だけでは行えない。見直す場合は、印刷教材（`nazorin-print.html`等）との実寸整合性への影響を含めて別途検討する。

### 4.3 判断根拠

- 判断基準は「実装頻度の多数決」ではなく、**1.3節に明記された値をそのまま正本とする**ことへ変更した（Phase17.5-Dの修正）。
- caption・heading・subheading・display・body-kidは、1.3節の値をrem単純換算してもPhase17.5-Aの実データとおおむね整合しており、この点は初版の分析と結論が一致する。
- bodyのみ1.3節の値（17px系）と実データ（16px系）に乖離があるが、乖離の原因は「Token値が実態に合っていない」のではなく「29アプリが1.3節に従っていない」側にあると判断を改めた。

### 4.4 見た目が変わる可能性

- 上記のいずれの値も、本文書の記述だけでは実装されない。値そのものをpxからremへ変更する作業はPhase17.6-Aで別worktree（`feature/typography-token-rem-pilot`）にてパイロット実施済みだが、**本Phase（17.5-D）ではそのworktreeに一切触れず、統合もしない**（14節参照）。
- 現時点でTypography Tokenを実際に参照しているコード箇所は0件（Phase17.4-AでTypography置換数0件と確認済み）のため、Token値をpxからremへ変更しても既存の表示に影響するアプリは存在しない。ただし将来Token参照が広がった後に値を変更すると影響範囲が拡大するため、値の確定（特にbodyの17px/16px論点）は横展開前に行うべきとする。

### 4.5 captionの下限リスク（Phase17.5-D新設）

- 1.3節では、注記（caption）の13pxは「これ未満は禁止」と明記された**最小値（下限）**であり、単なる目安ではない。
- rem換算候補は`0.8125rem`。ブラウザのルート文字サイズが既定の16pxである場合、計算結果は13pxちょうどとなり1.3節の基準を満たす。
- **リスク**: ユーザーがブラウザの既定文字サイズを16px未満に変更した場合、`0.8125rem`の計算結果が13px未満になり、1.3節の「これ未満は禁止」という下限を下回る可能性がある。
- **本Phaseでは以下を正式採用しない**: `font-size: max(13px, 0.8125rem)` のような、px下限とrem追随性を両立させる書き方。
  - 理由: `max()`でpx側の下限を強制すると、ユーザーが意図的に文字サイズを小さくしている場合でもそれを無視してしまう可能性があり、「ユーザーの文字縮小設定を尊重する」という3節・1節の原則3と衝突しうる。
  - 「13px下限を守ること」と「ユーザーのブラウザ設定への追随性を優先すること」（1節原則3）は、原理的に競合し得る。どちらを優先するかは本文書の範囲では決定せず、**次Phase以降の検証事項として保留する**。

---

## 5. line-heightの意味定義

Phase17.5-Aの調査（29アプリ・line-height 245件、うち unitless 240件）に基づき、以下の意味定義を正式仕様案とする。

| Token | 値 | 用途 | 用途に含まれないもの |
|---|---|---|---|
| `--dm-line-height-heading` | 1.4 | 見出し用途のみ。 | 説明文・ラベル・バナー文（実データでは1.4がこれらにも使われているが、意味が異なるため対象外とする） |
| `--dm-line-height-body` | 1.7 | 通常本文、説明文、モーダル本文、手順文など。 | 補助的な注釈・短いラベル |
| `--dm-line-height-body-kid` | 1.8 | 子ども向けの大きな本文、問題文、読みやすさを優先する文章。 | 大人向けUIの本文（`--dm-line-height-body`を使う） |
| `--dm-line-height-caption` | 1.6 | 注釈、補足、短い補助説明。 | 本文・説明文（実データでは1.6が`.modal-body`等の本文にも使われているが、意味が異なるため対象外とする） |

**値が一致していても用途が異なる場合は置換禁止とする。** Phase17.5-Aで確認した代表例:

- `line-height: 1.6` が `.modal-body`（モーダル本文）・`.quiz-question`（クイズ本文）など、実質的に「本文」用途で使われている例が45件中の多数を占める。これらは値がcaption Token（1.6）と一致していても、`--dm-line-height-caption` へは置換しない。
- `line-height: 1.4` が `.stg-desc`（設定説明文）・`.v-note`（注記）など「小さな説明文」用途で使われている例が21件中の多数を占める。値がheading Token（1.4）と一致していても、`--dm-line-height-heading` へは置換しない。

このような「値は一致するが意味が異なる」ケースは、**Token値そのものの見直し（4節同様の検討）または新規Tokenの追加（例: 説明文用の中間値）を将来検討する材料とし、機械的な値一致置換では解決しない。**

### 5.1 根拠の強さの区分（Phase17.5-D新設）

4つのline-height Tokenは、1.3節による裏付けの有無で2グループに分かれる。

**根拠が強い（1.3節に対応する固定値の記載がある）**:

- `--dm-line-height-body: 1.7` ── 1.3節「本文（基準）」の備考に「行間 1.7」と明記。
- `--dm-line-height-body-kid: 1.8` ── 1.3節「子ども向け本文」の備考に「行間 1.8」と明記。

**追加検証が必要（1.3節に直接対応する固定値の記載がない）**:

- `--dm-line-height-heading: 1.4` ── dev-rules 5.4節の「見出しの行間は1.3〜1.5」という範囲には収まるが、1.3節には見出し・大見出し・小見出しの行間についての固定値の記載がない。
- `--dm-line-height-caption: 1.6` ── dev-rules 5.4節には注記・caption専用の行間規定自体が存在しない。1.3節にも対応する記載がない。

Phase17.5-Aで検出した「1.4/1.6が名目上の用途（見出し／補助文）と異なる用途に多用されている」という実態は、この根拠の薄さと無関係ではない可能性がある。**現時点ではheading/caption line-heightの値を変更しない**が、将来の検証課題として記録する（15節）。

---

## 6. Typography Tokenの対象外

以下は、値が数値的に一致してもTypography Tokenの対象外とし、機械的に置換しない。

1. **絵文字や1文字表示の高さ調整としてのline-height:1**（例: `.header-icon`、`.big-kana`）。これはタイポグラフィ的な行間ではなく、単一グリフの垂直方向の詰め調整である。
2. **ボタン内文字の垂直中央寄せ**（例: `line-height: var(--min-tap)` や `line-height: 70px` のように、ボタン・タップ領域の高さに文字のline-heightを一致させて1行テキストを中央寄せする技法）。これはレイアウト技法であり、文章の読みやすさのための行間ではない。
3. **固定高さと同じpx値を指定したline-height**（2と同種、高さ＝line-heightという実装パターン全般）。
4. **レイアウト目的のline-height**（要素の高さそのものを決めるために使われているケース全般）。
5. **アイコンフォントや装飾表示の特殊指定**（絵文字サイズ調整・装飾的な記号表示など、文章表示ではないもの）。

Phase17.5-Aで確認した該当例: `yomikaki-app.html`の`--lh:2.2`（独自のテキストエリア向け行間変数、Token対象外ではないが意味が独立）、`sst-app.html`の`.action-btn`/`.qf-retry`（`line-height: var(--min-tap)`、上記2に該当）、`janken-app.html`の`.battle-hand`（`line-height: 70px`、上記2・3に該当）。

---

## 7. ルートfont-sizeの原則と例外

### 7.1 原則

`html`要素のfont-sizeを固定pxで上書きしない。ブラウザ既定（通常16px、ただしユーザー設定で変更可能）をそのまま尊重することで、remベースのTypography Tokenが正しくブラウザ／OSの文字サイズ設定に追随する。

### 7.2 例外調査対象

Phase17.5-Aの調査で、29アプリ中2アプリがルートfont-sizeを独自に上書きしていることが判明した。

#### schedule-app.html（Phase17.5-D: 記述を拡充）

- **現状**: `html{font-size:calc(var(--font-scale)*16px);}` — アプリ内蔵の文字拡大スライダー（`--font-scale`、既定値`1`）と連動する形でルートfont-sizeを動的に変更する設計。既定値`1`のとき`16px`となり、ブラウザ既定と一致する。
- **独自スケーリングは`html`だけに限定されない**: 実コードを確認したところ、`--font-scale`は`html`のfont-size以外にも、個別要素の幅・高さ・font-sizeに対して`calc(var(--font-scale)*90px)`・`calc(var(--font-scale)*1.6rem)`のような形で広く直接使用されている。つまりこのアプリの文字拡大は「ルートfont-sizeを変えてrem経由で全体に波及させる」方式と「個別要素に直接`--font-scale`を掛ける」方式が併用されている。
- **Token適用時の問題**: `html`のfont-size変更についてはremベースのTypography Tokenと方向性が一致し、大きな矛盾は生じないと考えられる。一方、個別要素に`calc(var(--font-scale)*Nrem)`のような形で直接Typography Tokenを組み込む場合、**ルート側のスケーリング（`--font-scale`経由）とrem自体のスケーリング（Token値がremであること）が二重にかかる可能性**があるため、Token適用時は二重スケーリングが発生していないかを個別に確認する必要がある。
- **単純に削除・統一しない**: この文字拡大機能はアプリ独自のアクセシビリティ機能であり、Typography Token導入を理由に削除・簡略化してはならない。

#### slideshow-sakusei.html

- **現状**: `html { font-size: calc(13px * var(--ui-scale, 1)); }` — ルートfont-sizeが**ブラウザ既定の16pxではなく13px**を基準にしている（`--ui-scale`の既定値1のとき13px）。
- **Token適用時の問題**: このアプリでは「1rem = 16px」という一般的な前提が成立しない。remベースのTypography Tokenをそのまま適用すると、他アプリと異なる実寸で表示される、またはToken側が想定する16px基準からのズレが生じる。
- **対応方針**: **Typography Token適用前に個別調査が必要**と明記する。13px基準を採用している設計意図（何らかのレイアウト都合または過去の実装判断）を確認したうえで、Token適用の可否・方法を別途判断する。この方針はPhase17.5-Bから変更しない。

---

## 8. calc()・clamp()・emの扱い

Phase17.5-Aの調査では、font-sizeにclamp() 123件・calc() 191件・em 155件が使用されていた。これらについて以下を原則とする。

- **calc()やclamp()を一律禁止しない。** 画面幅に応じた可変フォントサイズ（レスポンシブタイポグラフィ）など、意味のある可変設計であれば維持を認める。
- **文字サイズの可変設計に意味がある場合は維持可能。** 特に`clamp(最小値, 可変値, 最大値)`は、狭い画面幅での可読性確保と広い画面での過大表示防止を両立する技法であり、Token導入とは独立に評価する。
- **親要素に対する相対サイズが必要な場合はemを許容する。** 特定のコンポーネント内で親のフォントサイズに比例させたい場合（例: アイコンサイズを親テキストに追随させる）は、emの使用を妨げない。
- **Tokenへの置換によって既存の拡大縮小機能を壊さない。** `clamp()`や`calc()`の中身をTokenで単純に置き換えると、可変設計自体が失われる可能性があるため、置換前に個別確認を要する。schedule-app.htmlの`--font-scale`（7.2節）はこの原則の具体例でもある。
- **将来候補**: Tokenをclamp()やcalc()の構成要素として使う方法（例: `font-size: clamp(var(--dm-font-size-caption), 4vw, var(--dm-font-size-body))`）も将来的な選択肢として記録する。今回は具体案の設計・検証までは行わない。

---

## 9. font-family方針（Phase17.5-D改訂: 三系統の不一致を明記）

**現段階では共通Token化しない。** この結論はPhase17.5-Bから変更しないが、根拠をPhase17.5-Dで整理し直す。

### 9.1 三系統の不一致

font-familyについては、現在**互いに完全には一致しない3つの記載**が存在する。

- **A. Design System 1.3節**: 「見出し＝M PLUS Rounded 1c（700–900）、見出し・ボタン・数字にだけ使う」「本文＝BIZ UDPGothic / BIZ UDGothic（400・700）」の2書体制。「文字のかたちを学ぶ」教材面のみUDデジタル教科書体を例外許可（ただしWebでは使わない）。明朝体・極細ウェイト（300以下）は禁止。
- **B. dev-rules 5.4節**: `"BIZ UDゴシック", "BIZ UDPGothic", "Noto Sans JP", "Yu Gothic", "Meiryo", sans-serif` という単一のフォールバックスタックを「優先フォント」として提示。1.3節のような見出し／本文の書体分離は明記されていない。
- **C. 実装（29アプリ）**: Phase17.5-Aの調査で、29アプリ中37種類の異なるfont-family宣言が確認された。丸ゴシック系（M PLUS Rounded 1c、Rounded Mplus 1c等）、教科書体系、Hiragino系、Zen Maru Gothic、Kosugi Maru、Nunito（欧文）など多様であり、AにもBにも完全には一致しない。

### 9.2 共通Token化しない理由

- 実装（C）が37種類に分かれている一因は、各アプリの目的に応じて意図的に異なる書体が選ばれていると考えられる点にある。一括統一すると、教材としての読みやすさや、各アプリが意図した印象・用途適合性を損なう可能性がある。
- さらに、統一の基準とすべき「正本」自体がA・Bの間で揃っていないため、**現時点でCをAかBのどちらかに統一しようとしても、統一先の選定から別途の意思決定が必要**であり、本文書の範囲では扱わない。

### 9.3 将来の課題

- **どちらの記載（1.3節かdev-rules 5.4節か、あるいは両方の改訂か）を将来の正本とするかを、別Phaseで整理する。**
- 用途別分類の可能性として、以下の観点でカテゴリ分けした上で、部分的な共通Token化の余地を将来検討する。
  - **UI用フォント**（ボタン・ラベル等の操作系UI）
  - **教材本文用フォント**（学習コンテンツの本文）
  - **教科書体**（文字学習アプリ特有の、学校教科書に準拠した書体）
  - **数字や記号用フォント**（時計・計算等、数字の判読性が特に重要な場面）

この整理・分類作業は今回のPhaseでは調査・提案のみであり、具体的な分類作業・Token設計・A/Bどちらを正本とするかの決定は次Phase以降の課題とする。

---

## 10. Token置換の判定基準

TypographyをTokenへ置換できるのは、以下を**すべて**満たす場合のみとする。1つでも満たさない場合は置換禁止。

1. 意味がToken名と一致している（2節・5節の用途定義に基づく）。
2. 単位変更によって文字拡大追随性が低下しない（px→remの変更で追随性が向上する場合はよいが、rem→pxのように追随性を下げる変更は禁止）。
3. computed styleが意図した値になることを確認済みである。
4. レイアウト目的の指定ではない（6節の対象外に該当しない）。
5. 既存のアプリ内文字拡大機能を壊さない（7節のschedule-app.html等、独自機構を持つアプリは個別確認）。
6. 通常表示と拡大表示の両方を検証済みである（11節）。

**値が近いだけの置換は禁止する。** 例: 16px または 1rem を、「17px相当のbody Tokenに近いから」という理由だけで `--dm-font-size-body` へ置換しない。bodyの値は17px（1.0625rem）を正式方針として維持する（4.2節）。

この判定基準は、Phase17.4-Aで「Typography置換数0件」とした判断（rem→pxによる追随性低下懸念、値と意味の不一致）を正式ルール化したものである。

---

## 11. 検証要件

Typography Tokenを実際に適用する際は、以下をすべて確認する。

- 通常表示
- ブラウザ既定文字サイズでの表示
- ブラウザ文字サイズ拡大設定を変更した場合の表示
- 200%ページズーム時の表示
- 狭い画面幅での表示
- 文字の切れ・重なり・はみ出しの有無
- ボタンや入力欄の高さ（文字拡大時に窮屈にならないか）
- モーダルまたは設定パネル内の表示
- Switch Scanの動作
- Keyboard操作
- Focus表示
- High Contrast表示
- Console Error（0件であること）
- pageerror（0件であること）
- 改修前後のスクリーンショット比較（意図しない変化がないか）

これはPhase17.4-B（nazori-appパイロット）で実施した検証項目と同じ枠組みであり、Typography適用時も同水準の検証を要求する。

---

## 12. 移行順序（段階的移行計画）

一括置換は行わない。以下の7段階で進める。

| 段階 | 内容 | 状態（Phase17.10-B時点） |
|---|---|---|
| 第1段階 | Typography正式仕様の確定（本文書のレビュー・承認） | レビュー実施済み（Phase17.5-C・17.5-D）。body値は17px維持で確定方針。font-family三系統不一致等の未決事項を残したまま実装段階へ進める判断が下された |
| 第2段階 | generate.js内Token値をremへ変更するパイロット（値の変更のみ、参照箇所はまだ存在しないため表示への影響なし） | **完了。** Phase17.6-Aでパイロット実施、Phase17.6-Bでmainへ正式統合・全29アプリへ配布済み |
| 第3段階 | Tokenをまだ参照していない状態で、全29アプリへの配布と表示影響ゼロを確認（Phase17.3-Bと同型の配布・検証フロー） | **完了。** Phase17.6-Bで配布・表示影響ゼロを確認 |
| 第4段階 | `nazori-app.html`でTypography Tokenのパイロット適用（Phase17.4-A/Bと同型の1アプリ限定パイロット） | **完了。** Phase17.7-A/B/Cでnazori-app.htmlへ8箇所（セレクタ単位。CSSルール単位では7ルール）適用（詳細16.13節） |
| 第5段階 | ルートfont-sizeが標準的な（`html`のfont-sizeを上書きしていない）アプリへ限定して展開 | **一部実施。** Phase17.9-A/B/Cで6アプリ・9箇所（セレクタ単位。CSSルール単位では8ルール）へ横展開（詳細16.13節）。29アプリ全体からみれば一部にとどまり、多数の候補が個別確認待ちのまま残っている |
| 第6段階 | `schedule-app.html`など独自の文字拡大機能を持つアプリを個別対応 | 部分着手。schedule-app.htmlの`.hdr-title`・`.saved-meta`は`--font-scale`機構と独立した箇所として適用済み（16.6節・16.7節）。他の独自拡大アプリは未着手 |
| 第7段階 | `slideshow-sakusei.html`など非標準ルート基準（13px基準）を持つアプリを個別対応 | 未着手 |

各段階は前段階の完了・確認を経てから着手し、複数段階を同時に進めない。第4・5段階の実証結果とそこから確立した運用ルールは16節に記録する。

---

## 13. 周辺規定との交差（Phase17.5-D新設）

Typography Token（font-size・line-height）だけで文章の読みやすさを保証できるわけではない。1.3節には、Token化の対象外だが密接に関連する規定が併記されている。

- **1行の長さ**: 1.3節「全角20〜35字。超えたら折り返すか、段を分ける」。font-sizeやline-heightを変更すると1行に収まる文字数も変わるため、Token値の変更時はこの基準への影響も確認する。
- **ふりがな運用**: 1.3節「子ども向け画面は既定で全ルビ。設定でOFF。先生・保護者向けはルビなし」「学年で切替は採用しない」。font-size Token（特にbody-kid）を扱う画面はふりがな運用とセットで検証する。
- 上記2点は本文書のTypography Token仕様には含めない。1.3節を直接参照する。

また、1.6節「アクセシビリティ基準」のコントラスト規定「文字は4.5:1以上（**24px以上の大文字は3:1**）」は、`--dm-font-size-heading`・`--dm-font-size-body-kid`（いずれも24px相当）と直接関係する。24pxはWCAGの「大文字（Large Text）」判定の閾値と一致しており、**これらのTokenの値を将来変更する場合、閾値を跨ぐことでコントラスト要件（4.5:1 or 3:1のどちらが適用されるか）が変わる可能性がある**。Token値変更時は、この閾値との関係も再確認する必要がある。

---

## 14. Phase17.6-Aとの関係（Phase17.5-D新設）

Phase17.6-Aにて、別worktree（`for-all-children-to-learn-phase17-6-a`、branch `feature/typography-token-rem-pilot`）上でgenerate.js `buildDesignTokensHTML()`のfont-size系Token 6個を、本文書4.0節と同一のrem値へ変更するパイロットを実施済みである。

| Token | Phase17.6-Aでの変更 | 本文書4.0節の値 |
|---|---|---|
| `--dm-font-size-display` | `32px` → `2rem` | `2rem` |
| `--dm-font-size-heading` | `24px` → `1.5rem` | `1.5rem` |
| `--dm-font-size-subheading` | `20px` → `1.25rem` | `1.25rem` |
| `--dm-font-size-body` | `17px` → `1.0625rem` | `1.0625rem` |
| `--dm-font-size-body-kid` | `24px` → `1.5rem` | `1.5rem` |
| `--dm-font-size-caption` | `13px` → `0.8125rem` | `0.8125rem` |

一致していることを確認した。line-height系Tokenは対象外（Phase17.6-Aでも変更していない）。

**本Phase（17.5-D）ではPhase17.6-A worktree内のファイル（generate.js・29アプリHTML）には一切触れず、統合もしない。** Phase17.6-Aの差分は当該worktree上に未commitのまま保持される。統合の要否・タイミングは別Phaseで判断する。

---

## 15. 未決定事項（次Phaseへの引き継ぎ）

- 2節: `--dm-font-size-heading` と `--dm-font-size-body-kid` の値の重複（ともに24px/1.5rem）は意図的な偶然の一致と整理したが、将来どちらかの値を独立に変更する際の運用ルール（Token名を分離したまま維持する）を、実際に変更が必要になった時点で再確認する。
- 4.5節: caption Tokenの13px下限リスクと、ユーザーのブラウザ文字サイズ設定への追随性が競合する可能性への対応（`max()`の採用可否を含む）。
- 5.1節: `--dm-line-height-heading`（1.4）・`--dm-line-height-caption`（1.6）は1.3節に対応する固定値の記載がなく、根拠がbody/body-kidより弱い。実データで用途との不一致が多いことも踏まえ、値の再検証が必要。
- 9節: font-family方針のA（1.3節）・B（dev-rules 5.4節）・C（実装37種類）の不一致をどう解消するか。どちらを正本とするか、または両方を改訂するか。UI用／教材本文用／教科書体／数字記号用の4分類の具体的な調査・設計。
- 8節: Tokenをclamp()/calc()の構成要素として使う設計の具体案。
- 13節: Token値変更時の1行の長さ・ふりがな運用・コントラスト閾値への影響を、実際の値変更が発生するタイミングで個別に再検証する。
- 12節: 第1段階（本文書の正式承認）の完了後、第2段階（Phase17.6-Aのパイロット差分の統合）を別Phaseで判断する。

---

## 16. Typography Token横展開ルール（Phase17.7-C〜17.10-A実証、Phase17.10-Bにて正式記録）

### 16.0 本節の位置づけ

10節「Token置換の判定基準」・11節「検証要件」は、実際にTokenを適用する前（Phase17.5-D時点）に定めた原則である。Phase17.7-C（nazori-appパイロット）からPhase17.9-C（高確度パターン横展開）にかけて、これらの原則を実データへ適用した結果、10節・11節だけでは足りない具体的な運用手順（非標準ルートの検出方法、レスポンシブ上書きの扱い、動的生成要素の確認方法など）が判明した。本節はその実証結果を恒久的な運用ルールとして記録するものであり、10節・11節を置き換えるものではなく、その実務手順を補強するものである。

本節に記載する数値・成功例・除外例は、いずれもPhase17.7-CからPhase17.9-Cで実際にmainへcommit・pushし、GitHub Pages公開版で確認済みの事実に基づく。推測・未検証の内容は含まない。

**現時点の適用範囲（過大評価防止のための明記）**: 本節時点で実際にTypography Tokenを適用済みなのは **7アプリ・17セレクタ（1ルールに複数セレクタを含む2箇所を1行として数えるdiff行数ベースでは15行）** にとどまる。29アプリ全体で3,000件を超えるfont-size宣言が存在する中の一部にすぎず、「全29アプリへのTypography Token適用完了」を意味するものではない。詳細な内訳は16.13節に記載する。

### 16.1 意味優先の原則（実務手順）

Typography Tokenは、現在の数値が近いかどうかではなく、**HTML上の意味・役割**に基づいて適用する。セレクタ名だけで意味を決定してはならない。適用前に必ず次を確認する。

1. HTML要素（タグ名、`h1`〜`h6`等の見出しレベルを含む）
2. 親要素
3. 周辺文言
4. 画面内での役割（画面全体の主タイトルか、パネル内の一部か等）
5. JavaScriptによる動的生成内容（`textContent`・テンプレートリテラル等）
6. `aria-live` / `role="status"` / `role="alert"`との関係
7. 子ども向け主要表示か、補助表示か

代表例:

- `.header-title`でも、アプリ全体のヘッダーに表示される主タイトルであればheading候補となる（nazori-app、schedule-app、sst-app）。
- `.help-header-title`（kimochi-board.html）は名前に「header」を含むが、実際はヘルプダイアログ内の見出しであり、アプリ全体の主タイトルとは役割が異なるため除外した。
- `.header-sub`（nazori-app.html）は小さい文字（0.75rem）であっても、内容を確認するとアプリ名直下のタグライン・ブランドsubtitleであり、caption（注釈・補足）の意味とは完全には一致しない境界事例だった。Phase17.7-Bのレビューでcaption適用を取り消し、`0.75rem`へ復元した（Phase17.7-C）。

### 16.2 高確度パターン（実証済み3種）

Phase17.8-Bで抽出した候補のうち、Phase17.9-A〜Cで実際に横断適用まで進めた3パターンを記録する。

#### 16.2.1 アプリ全体ヘッダータイトル

- 対象: アプリ全体の主タイトル。1画面1箇所。ヘッダー内に表示されるアプリ名。
- 推奨Token: `var(--dm-font-size-heading)`
- 期待computed value: `24px`（標準16pxルート時）
- 成功例: nazori-app.html `.header-title`、schedule-app.html `.hdr-title`、sst-app.html `.hdr-title`
- 除外例: register-app.html `.header-title`（`calc(var(--font-size-base)*1.3)`使用）、kimochi-board.html `.help-header-title`（意味不一致、16.1節参照）

#### 16.2.2 標準フォームコントロール

- 対象: 通常の文字入力、通常のselect、通常のtextarea。入力値・選択値・placeholderの表示文字。
- 推奨Token: `var(--dm-font-size-body)`
- 期待computed value: `17px`（標準16pxルート時）
- 成功例: nazori-app.html `input[type="text"]` / `select`、yomikaki-app.html `textarea`、okane-app.html `.custom-modal input[type="text"]` / `.custom-modal input[type="number"]`、shiritori2.html `.scan-sub select`
- **標準フォームコントロールであっても、特殊入力・特大数値・ゲーム操作用・独自スケール依存のものは対象外とする**（例: tokei-app.htmlの`.timer-input-value`のような特大数値表示、nazori-app.htmlの`.char-input-large`のような1文字表示的入力）。

#### 16.2.3 補助メタ情報

- 対象: 日付、時刻、所要時間、件数、トラック数、記録番号など、主情報に付随する補足情報。
- 推奨Token: `var(--dm-font-size-caption)`
- 期待computed value: `13px`（標準16pxルート時）
- 成功例: nazori-app.html `.rec-meta`、schedule-app.html `.saved-meta`、ongaku-app.html `.rec-item-meta` / `.comp-item-meta`
- 除外例: matching-app.html `.set-meta`（`em`指定）
- **スコア・結果・警告・主要状態・`aria-live`通知は、セレクタ名に`meta`が含まれていても自動適用しない。**

### 16.3 自動置換禁止条件

以下の指定は、意味が一致していても自動的にTokenへ置換してはならない。

```text
calc()
clamp()
em
%
独自CSS変数
--font-scale
--ui-scale
--font-size-base
```

これらはレスポンシブ調整・独自文字拡大機能・親要素との相対関係・アクセシビリティ設定・印刷レイアウト・子ども向け特殊表示のいずれかと関連している可能性があるため、機械的な置換対象から除外する。**固定pxまたは固定remであっても、16.1節の意味確認は省略できない。** 固定値であることは適用の必要条件であって十分条件ではない。

### 16.4 ルートfont-sizeの事前確認

Token適用前に、対象アプリのCSSで必ず次の両方の記法を確認する。`:root`のみ、または`html`のみを検索するだけでは見逃す場合がある。

```css
:root {
  font-size: ...;
}
```

```css
html {
  font-size: ...;
}
```

代表例:

- `nazorin-print.html`: `:root { font-size: 15px; }`。`.92rem`の実測値が`13.8px`となり、一般的な16px基準の想定（`14.72px`）と一致しなかった。この差異が非標準ルートの発見につながった（Phase17.9-B）。`:root`記法での指定であり、`html{font-size:...}`だけを検索する手順では見逃していた。
- `slideshow-sakusei.html`: `html { font-size: calc(13px * var(--ui-scale, 1)); }`
- `schedule-app.html`: `html { font-size: calc(var(--font-scale) * 16px); }`（既定値`1`のため実質標準16px相当）

**remの記述値だけで16px基準のpx換算を行い、それを根拠に適用可否を判断してはならない。必ずブラウザでcomputed valueを実測する（16.5節）。**

### 16.5 computed value確認

Tokenを記述しただけでは適用完了としない。通常幅と狭幅（375×667）の両方で、ブラウザのcomputed styleを実測する。

標準16pxルート時の期待値:

```text
heading:     24px
subheading:  20px
body-kid:    24px
body:        17px
caption:     13px
```

ルートfont-size・メディアクエリ・アクセシビリティ拡大上書きの影響でこの値と異なる場合は、それが意図した既存挙動（16.6節・16.7節）か、誤適用かを個別に判断する。数値の見かけの換算だけで完了とみなさない。

### 16.6 レスポンシブ上書きの扱い

基本ルールとメディアクエリ内の上書きは分けて扱う。

- 基本ルールのみをTokenへ変更する。
- 既存の狭幅上書き（メディアクエリ内の同一セレクタへの別のfont-size指定）は変更しない。
- 狭幅で既存の上書き値が維持されることをcomputed styleで確認する。
- 基本ルールと上書きルールの一括置換は禁止する。

代表例（schedule-app.html）:

```css
.hdr-title {
  font-size: var(--dm-font-size-heading);
}

@media (max-width: 560px) {
  .hdr-title {
    font-size: 1rem;
  }
}
```

この方法により、通常幅では`24px`、狭幅（375px等、`560px`未満）では既存の`16px`という挙動をそのまま維持できることをPhase17.9-Aで確認した。**レスポンシブ上書きが存在すること自体を、即座の除外理由にしない。** 上書きを維持したまま基本値のみを変更できるかを個別に検討する。

### 16.7 アクセシビリティ文字拡大との関係

以下のような既存の上書きが対象セレクタに存在する場合、自動適用しない。

```text
body.large-text
body.font-large
body.font-xlarge
[data-font]
--font-scale
--ui-scale
```

これらの機構を持つ要素は、通常状態だけでなく拡大状態も含めた個別調査が必要である。以下のアプリで同種の機構を確認済みである。

```text
timetable-app.html
schedule-app.html
slideshow-sakusei.html
janken-app.html
shiritori2.html
register-app.html
okane-app.html
```

**アプリに独自文字拡大機構が存在していても、対象セレクタがその機構と独立していることを確認できた場合に限り適用可能とする**（例: schedule-app.htmlの`.hdr-title`・`.saved-meta`は`--font-scale`と無関係な単純ルールであることを確認したうえで適用した）。

### 16.8 line-heightの扱い

Typography Token適用時は、**原則としてfont-sizeのみを変更し、line-heightは同時に変更しない**。

代表例（nazori-app.html）:

- `.modal-body`: `line-height: 1.6`を変更せず維持した。
- `.stg-desc`: `line-height: 1.4`を変更せず維持した。

unitless line-heightは、font-size変更後もその比率のまま自動的に追随することを実測確認済みである（例: `.modal-body`はfont-size 16px→17pxの変更に伴いcomputed line-heightが25.6px→27.2pxへ、比率1.6を保ったまま追随した）。**line-height自体をTypography Tokenへ置換する判断は、本節の対象外とし、別の調査・別Phaseとして扱う**（5節・5.1節参照）。

### 16.9 動的生成要素の確認方法

JavaScriptで生成される要素は、静的HTMLの読解だけでは用途や実際の表示を確認できない場合がある。次の手順で確認する。

1. JavaScriptの`textContent`・`innerHTML`・テンプレートリテラルを読み、表示される文言の性質を確認する。
2. 可能であれば実データを投入し、実際に画面へ表示させて確認する。
3. 実データ投入が困難な場合（IndexedDB等の複雑なデータ層を持つ場合等）は、同一クラス名を持つ一時的なprobe要素を生成し、computed styleのみを確認する代替手段を用いる。
4. probe要素による確認にとどめた場合は、**実際の画面表示までは確認していないという限界を報告に明記する。**
5. `aria-live`等の主要状態通知との関係を確認する（16.1節）。

代表例: schedule-app.html `.saved-meta`、ongaku-app.html `.rec-item-meta` / `.comp-item-meta`。いずれもJavaScriptによる動的生成であり、テンプレートリテラルの読解と（可能な範囲での）実データ投入により用途を確認した。

### 16.10 CSSルール分割の扱い

同一のCSSルールに、意味の異なる複数セレクタがカンマ区切りでまとめられている場合、そのうち一部だけを機械的に変更してはならない。全セレクタが同一Tokenの意味条件を満たすか確認する。

一部のセレクタだけが対象で、意味の異なるセレクタを分離するためにCSSルールの分割が必要になる場合は、次の原則に従う。

- そのPhase・作業では変更しない。
- ルール分割（セレクタの再構成）は別Phaseの構造変更作業として扱う。
- font-sizeのToken置換と、セレクタ構造の再構成を同時に行わない。

### 16.11 変更後の必須確認

Typography Token適用後は、最低限次の項目を確認する。

**表示確認**: 通常幅／375×667／文字切れ／折り返し／横スクロール／入力欄の高さ／アイコンとの重なり／主情報との視覚階層／placeholder／select選択値／動的生成情報の表示。

**操作確認**: Keyboard／Tab移動／Focus／Switch Scan／High Contrast／独自文字拡大機能／Console Error（0件）／pageerror（0件）。

**git確認**:

```bash
git status --short
git diff --stat
git diff
git diff --check
```

font-size以外の変更が含まれていないことを確認する。

### 16.12 適用しない判断

以下のいずれかに該当する場合、**「変更しない」が正式な成功判断である。** Typography Tokenの適用数を増やすこと自体を目的にしてはならない。

- 意味がToken名と一致しない。
- 独自スケールに依存している。
- computed valueを確定できない。
- 非標準root font-sizeの影響が未解決である。
- CSSルール分割が必要である。
- 主要表示と補助表示が同一ルールに混在している。
- 既存のレスポンシブ挙動を維持できない。
- アクセシビリティ機能への影響を否定できない。
- 実際の画面表示を確認できない（probe確認のみで実表示未確認の場合を含む）。
- 候補数が少なすぎて共通ルール化の根拠にならない。
- 調査コストが大きく、別Phaseとして扱うべき規模である。

### 16.13 現在の適用範囲（Phase17.10-A時点の実績）

過大評価を避けるため、実際にTypography Tokenを適用済みの箇所を明記する。

| Token | 適用件数 | 適用アプリ・セレクタ |
|---|---|---|
| `--dm-font-size-heading` | 4件 | nazori-app.html `.header-title` / `.modal-title`、schedule-app.html `.hdr-title`、sst-app.html `.hdr-title` |
| `--dm-font-size-body` | 7件（うちnazori-appの`input[type="text"], select`とokane-appの`input[type="text"], input[type="number"]`はそれぞれ1ルール2セレクタのため、diff行数では2件と数える） | nazori-app.html `input[type="text"], select` / `.modal-body`、okane-app.html `.custom-modal input[type="text"], .custom-modal input[type="number"]`（1ルールで2セレクタ）、shiritori2.html `.scan-sub select`、yomikaki-app.html `textarea` |
| `--dm-font-size-caption` | 6件 | nazori-app.html `.rec-meta` / `.rec-img-label` / `.stg-desc`、schedule-app.html `.saved-meta`、ongaku-app.html `.rec-item-meta` / `.comp-item-meta` |

適用アプリは合計7アプリ（nazori-app、schedule-app、sst-app、okane-app、shiritori2、yomikaki-app、ongaku-app）。**`--dm-font-size-subheading`・`--dm-font-size-body-kid`・`--dm-font-size-display`は、本節時点でいずれも実アプリへの適用例が0件である。**

**未適用・保留の候補が多数残っている。** Phase17.10-A時点でmainの実コードを機械的に再走査した結果、Token未適用（`var(--dm-font-size-*)`未使用）のfont-size宣言のうち、`header-title`/`hdr-title`を含むセレクタが5件、`input`/`select`/`textarea`を含むセレクタが50件、`-meta`を含むセレクタが1件、いずれも現在mainに残っている。header-title系5件は、いずれもnazori-app.htmlの`body.large-text .header-title`（文字拡大時上書き）、register-app.htmlの`.header-title`（`calc(var(--font-size-base)*1.3)`）・`.print-header-title`（印刷用途で意味が異なる）、schedule-app.htmlの`.hdr-title`狭幅上書き（16.6節で意図的に維持）、kimochi-board.htmlの`.help-header-title`（16.1節で意味不一致と判定済み）であり、いずれも本節の除外条件に該当することを個別確認済みである。form系50件・meta系1件は、`calc()`/`clamp()`/`em`/独自CSS変数等による技術的除外に該当するものと、意味・用途の個別確認が済んでいないものが混在しており、件数の内訳を一括りに集計していない。詳細はPhase17.10-Aの調査6を参照する。

### 16.14 subheading・body-kid・modal-titleの扱い（未確立）

以下は、本節で「高確度パターン」として一般ルール化するには至っていない。

- **subheading**: bosai-app.html `.mechanism-box h3`（20px、`<h3>`タグ、値・意味とも`--dm-font-size-subheading`と一致）という個別の確定候補が1件見つかっているが、パターンとしての横断実証は行っていない。1件のみの実装可否は、今後の個別パイロットとして別途判断する。
- **body-kid**: 子ども向け主要文言（問題文等）とTokenの値（24px）が一致する固定値の実例は、Phase17.8-A〜17.9-Cの調査を通じて確認できていない。timetable-app.htmlの`.quiz-q`のように役割は一致しても値が一致しない（20px、subheadingの値と数値上一致するにすぎない）事例が見つかっているが、これは「値が一致するが意味が異なる」の逆パターン（意味は近いが値が異なる）であり、機械的な適用根拠にはならない。
- **modal-title**: アプリ全体ヘッダータイトルとは別に、モーダル・ダイアログの見出しとして厳密に`.modal-title`という名前のセレクタを持つアプリが7件（cup_game・kyou-no-kiroku・nazori-app［適用済］・register-app・schedule-app・sst-app・time-timer）確認されているが、値は24px（cup_game）、`1.2em`相対値（kyou-no-kiroku）、`calc(var(--font-size-base)*1.2)`（register-app、独自スケール依存）、約17.6px（schedule-app）、18px（sst-app）、`calc(1.2em * var(--font-scale))`（time-timer、独自スケール依存）とばらばらであり、共通ルールとして一般化できていない。加えて`.custom-modal-title`（okane-app）・`.app-modal-title`（okane-app、独自スケール依存）・`.result-modal-title`（app-register）・`.smodal-title`/`.ph-modal-title`/`.help-modal-title`/`.prof-modal-title`（gaze-keyboard）のように、名前が異なる類似セレクタも複数存在し、これらを同一パターンとして扱えるかどうかも未確認である。

これら3件は、いずれもPhase17.10-Aの「調査7」で次フェーズ候補として比較検討されたものであり、本節の時点では正式な高確度パターンに昇格していない。

---

## 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| v3.0（初版・ドラフト） | 2026-07-31 | Phase17.5-Bにて新規作成。Phase17.5-A（全29アプリ調査）を根拠に、Typography正式仕様案として起草。未承認・未統合。 |
| v3.0（改訂1・レビュー待ち） | 2026-07-31 | Phase17.5-Cのレビュー結果を反映しPhase17.5-Dにて改訂。主な変更: (1)Design System 1.3節に既存のTypography仕様があることを訂正・明記し「Design System全体のVer.3.0文書が存在しない」ことと区別、(2)body Tokenを1remへ変更する案を撤回し17px(1.0625rem)維持を正式方針化、(3)1.3節との対応表（4.0節）を新設、(4)dev-rules 5.4節との関係を整理、(5)heading/body-kidの値重複の理由を9.8.2節の設計思想と関連付けて整理、(6)caption 13px下限リスクを明記（4.5節）、(7)line-heightの根拠の強さを区分（5.1節）、(8)schedule-app.htmlの個別要素calc()使用を追記、(9)font-family三系統不一致を明記（9節)、(10)1.3節の周辺規定（1行の長さ・ふりがな）および1.6節コントラスト基準との交差を追記（13節）、(11)Phase17.6-Aのrem化パイロットとの整合確認を追記（14節）。未承認・未統合。 |
| v3.0（改訂2・レビュー待ち） | 2026-07-31 | Phase17.7-C〜Phase17.9-CでのTypography Token横展開実証（7アプリ・17セレクタ）とPhase17.10-Aの総括レビューを踏まえ、Phase17.10-Bにて改訂。主な変更: (1)12節の移行順序ステータス表を実際の進捗（第2〜4段階完了、第5・6段階一部実施）に更新、(2)新設16節にて実証済みの運用ルールを正式記録。内容は意味優先の原則の実務手順（16.1）、高確度パターン3種の成功例・除外例（16.2）、自動置換禁止条件（16.3）、ルートfont-size事前確認（16.4）、computed value確認（16.5）、レスポンシブ上書きの扱い（16.6）、アクセシビリティ文字拡大との関係（16.7）、line-heightの扱い（16.8）、動的生成要素の確認方法（16.9）、CSSルール分割の扱い（16.10）、変更後の必須確認（16.11）、適用しない判断（16.12）、(3)現時点の適用範囲（7アプリ・17セレクタにとどまり全29アプリ完了ではないこと）を数値で明記（16.13）、(4)subheading・body-kid・modal-titleは未確立のまま次Phase候補として記録（16.14）。コード変更なし、文書化のみ。未承認・未統合。 |
