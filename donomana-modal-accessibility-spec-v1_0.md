# どのまな モーダルアクセシビリティ仕様書 v1.1

- 版: v1.1（初版から改訂・**承認済み**・レビュー完了・main統合済み）
- 発行: 2026年8月（初版）
- 作成日: 2026-08-01（初版）／改訂日: 2026-08-02（v1.1）
- 承認日: 2026-08-02（v1.1）
- 位置づけ: `donomana-design-system-v2_0.html`（共通デザインシステム Ver.2.1）を**上位方針とする実装詳細の補足文書**。Design Systemの内容を置き換えるものではない（詳細は2章）。
- 根拠調査: Phase18.1〜18.7（初版根拠：matching-app・schedule-app・time-timer・register-appの調査・実装・検証・Design System整合）／Phase18.9〜18.11（v1.1改訂根拠：matching-app・schedule-appの共通A11yパネル対応Focus Trap実証、schedule-appのワンホップ検出と解消、okane-app部分実装調査。いずれも実測またはコードレビューに基づく）
- 起草: Phase18.7-B／改訂: Phase18.11-B／承認: Phase18.11-E
- 参照元: `donomana-design-system-v2_0.html` 9.9.1 稼働中コードとの衝突ログ #1・#6・#8

> **本文書はv1.1として承認済みであり、既存モーダル改善の共通基準として運用する。** 未決定（U）とした事項は、将来Phaseでの追加実証を経て確定する前提で継続管理する。**本文書単独で新規モーダルの採用を許可するものではない。** 新規モーダルの採用可否は、引き続き `donomana-design-system-v2_0.html` 7.5節・7.6.3節が判断する。本文書に記載された各アプリの実装状況（20章参照）は、全アプリでの全要件適合完了を意味しない。generate.js・アプリHTML・Design System・dev-rules・Typography仕様書は本文書のみを理由に変更してはならない。

---

## 0. 本文書の背景

Phase18.1〜18.5にて、matching-app.html（how-ov／settings-ov／edit-ov）とschedule-app.html（new-modal／print-modal／img-modal）の2アプリ・計8モーダルへ、Keyboardアクセシビリティ改善（ARIA構造・初期フォーカス・Focus Trap・Escape制御）と、matching-appについては背景inert・Switch Scan候補限定を実装・実測検証・公開した。

Phase18.6-A〜Cにて、この実装結果とDesign Systemの既存方針（7.5節「モーダル・ポップアップ禁止」、7.6.3節「PINロック正式例外」）との関係を調査し、**「新規UIとしてのモーダル採用」と「既存モーダルのアクセシビリティ改善」は別の判断軸である**という整理を、Design System 9.9.1 #8として正式に記録した。

Phase18.7-Aにて、この整理に基づき、新規仕様書が担うべき責任範囲・要件分類・章構成の草案を確定した。本文書はその結果を反映した初版ドラフトである。

---

## 1. 目的

本文書は、既存の非PINモーダル（および将来Design System上で正式例外と認められた新規ダイアログ）について、アクセシビリティ実装の詳細——HTML/ARIA構造・初期フォーカス・Focus Trap・Escape制御・背景操作抑制・Switch Scanとの関係・動的DOMへの追随・検証手順・横展開の進め方——を記録し、今後の実装作業の参照点とすることを目的とする。

本文書は、matching-app.html・schedule-app.htmlという2アプリでの実証結果を主な根拠とする。2アプリだけでは実証しきれない事項は「未決定」として明示し、将来Phaseでの追加実証を経てから確定させる。

---

## 2. 文書の位置づけ

### 2.1 正本と参照関係

| 文書 | 関係 | 参照箇所 |
|---|---|---|
| `donomana-design-system-v2_0.html` | **上位方針（正本）** | 7.5節（新規モーダル禁止）／7.6.3節（PINロック正式例外）／9.9.1 #1（既存製品は改善を続ける）／9.9.1 #6（PINロックのみ正式例外）／9.9.1 #8（新規採用制限と既存改善の分離） |
| `donomana-typography-spec-v3_0.md` | **見出しタイポグラフィの正本** | 16.15節（modal-title意味分類）／16.16節（モーダル階層 全29アプリ横断調査） |
| `donomana-dev-rules-v1.0-revised.md` | 参照元（17章に追加済み） | 17章「今後の関連文書」に本仕様書への参照が既に追加されている（`e2ab484`）。本仕様書の承認（Phase18.11-E）に合わせ、dev-rules側の状態説明も更新する |
| `CLAUDE.md` | 対象外 | リポジトリのビルド・アーキテクチャガイドであり、本文書からもDesign Systemからも参照しない |

本文書はDesign Systemの下位に位置し、Design Systemの方針と矛盾する内容を含んではならない。矛盾が判明した場合はDesign Systemを正本とし、本文書側を修正する。

Typography（見出しの文字サイズ等）については、意味分類・階層の内容を本文書に複製せず、`donomana-typography-spec-v3_0.md` 16.15節・16.16節を参照する。

### 2.2 責任範囲の分担

- **Design System**：新規UIとしてモーダルを採用してよいかどうかの上位方針（7.5節・7.6.3節・9.9.1）を保持する。この判断は本文書に委譲されない。
- **本文書**：既存モーダル（および正式例外と認められたダイアログ）の、アクセシビリティ実装詳細を扱う。
- **dev-rules**：実装作業時の簡易チェックリストとして、本文書を参照する（参照追加は後続Phase）。
- **Typography仕様書**：見出しの意味分類・文字サイズTokenを扱う。本文書とは分離し、複製しない。

---

## 3. Design Systemとの関係

Design System `donomana-design-system-v2_0.html` に記載されている以下の方針は、本文書によって変更されない。

1. **新規モーダルの採用は7.5節により原則禁止**である。「唯一の例外：取り消せない操作の確認だけ」という条件付き例外を除き、設定・ヘルプ・確認・結果表示を新規に実装する場合は7.6節の非モーダル共通UIコンポーネント（設定パネル・インライン確認・ヘルプ・結果表示）を優先する。
2. **PINロックは7.6.3節の正式例外**である。Design System上で正式に新規採用してよいモーダル型コンポーネントは、現時点でPINロックのみである。
3. **PIN以外の用途で新規にモーダル型UIが必要になった場合は、自動的に許可されない。** Design System 9.9節への個別記録を経て、正式例外として判断される必要がある。
4. **既存の非PINモーダルは、9.9.1 #1・#8に基づき、廃止を急がず改善を続ける対象**として扱われる。
5. **モーダルUIを新規に採用するかどうかの判断と、既存モーダルへ適切なARIA属性を付与するかどうかの判断は、別の判断軸**である（9.9.1 #8）。

**本文書は、上記1〜3のモーダル新規採用可否そのものを判断する文書ではない。** 本文書が扱うのは、4・5の枠組みのもとで「既に存在する非PINモーダル」および「将来Design Systemが正式例外と認めた新規ダイアログ」に対する、アクセシビリティ実装の詳細のみである。

---

## 4. 用語

- **モーダル**：`role="dialog"`（またはネイティブ`<dialog>`）を用いて表示される、背景操作を意図的に制限するオーバーレイUI。
- **既存非PINモーダル**：本文書作成時点（2026-08-01）で、PINロック以外の用途で既に稼働している`role="dialog" aria-modal="true"`実装。4アプリ（matching-app・schedule-app・register-app・time-timer）で計16件を確認済み（22章参照）。
- **okane-app.html（部分実装）**：ARIA・初期フォーカス・Focus Trap・共通A11yパネル優先のEscapeガードの改善は既に実装されているが、matching-app・schedule-appで実証した共通A11yパネル対応Focus Trap（16章）は未適用、背景操作抑制も未実装である。上記「4アプリ16件」の実証集計には含めない。詳細は20.4節を参照。
- **Focus Trap**：モーダル表示中、Tab／Shift+Tabによるフォーカス移動をモーダル内の操作要素に閉じ込める仕組み。
- **背景操作抑制**：モーダル表示中、背景側の要素への実操作（クリック・フォーカス）を防ぐ仕組み。実装方式は問わない。
- **Switch Scan**：単一スイッチ／2スイッチによるスキャン走査型の操作支援機構。アプリごとに実装方式が異なる。
- **要件分類記号**（M/R/C/U/A）：7章で定義する。

---

## 5. 要件分類

本文書では、各要件に以下いずれかの記号を付ける。

- **M：最低要件**——既存モーダル（対象範囲内）を改善する際、必ず満たすべき事項。2アプリ（matching-app・schedule-app）双方で実証済みのものに限定する。
- **R：推奨要件**——実装を推奨するが、必須としない事項。実装するかどうかはモーダルの性質（意味分類）によって判断が変わる。
- **C：条件付き要件**——特定の条件（例：Switch Scanがモーダルと同時動作するアプリの場合のみ）で必須になる事項。
- **U：未決定**——2アプリの実証だけでは根拠が不十分で、将来Phaseでの追加実証を経て確定させる事項。現時点では最低要件にも推奨要件にもしない。
- **A：アプリ固有**——達成すべき結果は共通でも、実装方式（具体的なコード・セレクタ・変数名）はアプリのHTML構造に依存し、共通化しない事項。

M/R/C/U/Aは混同せず、各項目の末尾に記号を明記する。記号のない記述は、原則または背景説明であり要件そのものではない。

---

## 6. 適用範囲

### 6.1 主対象

- 既存の非PINモーダル（`role="dialog"`を使うdivオーバーレイ）
- Design Systemで正式例外と判断された新規ダイアログ

### 6.2 参考対象（本文書の要件をそのまま適用しない）

- PINロックモーダル——要件はDesign System 7.6.3節が正本。本文書は通常モーダルとの相違点（Escapeバイパス禁止等）の記録にとどめる
- 非モーダル設定パネル（`donomanaA11yPanel`等、`aria-modal`を持たない構造）——**本仕様書の全要件をそのまま適用しない**。7.6.1節の設計（背景を全面的に覆わない）に従う
- 共通A11yパネル——16章参照。**本仕様書の全要件をそのまま適用しない**
- ネイティブ`<dialog>`——4アプリとも未使用（注記：本文書作成時点でネイティブdialogを使用しているアプリはない）
- OS／ブラウザの外部ダイアログ（`window.print()`のダイアログ、OSファイル選択等）——17章参照。アプリ側の制御対象外

### 6.3 未決定または別扱い

- 称賛・完了演出（19章、正式要件は未決定）
- `role="alertdialog"`（23章、未決定）
- 視線入力固有要件（23章、未決定）

---

## 7. 最低要件（M）

以下は、matching-app・schedule-appの2アプリで実証済みの範囲に限定した最低要件である。対象は6.1節の主対象モーダルとする。

1. **M** — `role="dialog"`を使用する。ネイティブ`<dialog>`要素も選択肢になりうるが、**本文書作成時点で4アプリ（matching-app・schedule-app・register-app・time-timer）のいずれも使用していない**ため、具体的な適用方法は未検証。
2. **M** — `aria-modal="true"`を付与する。
3. **M** — `aria-labelledby`で見出し要素を参照する。見出しが存在しない特殊なモーダルでは`aria-label`を使うことができるが、**見出しそのものを省略することは推奨しない**。
4. **M** — 見出しに一意なidを付与する。
5. **M** — Tab／Shift+TabのFocus Trapを実装する（詳細は8章）。
6. **M** — モーダルが閉じた状態では、モーダル内の要素を操作できない状態にする（実装方式は8章・9章参照）。
7. **M** — Switch Scanが存在するアプリでは、KeyboardとSwitch Scanを競合させない（詳細は11章）。
8. **M** — モーダル内の動的DOM（追加・削除される要素）へFocus Trapが追随する（詳細は12章）。
9. **M** — Focus Trapの候補が0件になっても例外を発生させず、安全にreturnする。
10. **M** — 実装後、Console Errorが0件であることを確認する。
11. **M** — 実装後、pageerrorが0件であることを確認する。
12. **M** — 通常幅（1280px相当）で検証する。
13. **M** — 375×667で検証する。
14. **M** — commit・merge・push後、公開版で再検証する。

---

## 8. 初期フォーカス

初期フォーカスを設けること自体は **R（推奨要件）** とする。

### 8.1 正式記載可能な分類

- **R** — 説明中心のモーダル：見出しへ初期フォーカスする
- **R** — 設定に関するモーダル：見出し、または最初の安全な操作要素へ初期フォーカスする
- **R** — 破壊的確認（取り消しにくい操作の確認）：キャンセル等の安全側操作へ初期フォーカスする
- **R** — 印刷／ファイル選択に関するモーダル：見出し等、実行を伴わない要素へ初期フォーカスする

### 8.2 未決定（U）

- 入力モーダルの最初の入力欄への初期フォーカス
- PIN認証モーダルの初期フォーカス（最初の入力またはキーパッド）
- 称賛・完了演出モーダルの初期フォーカス
- 操作要素0件のモーダルの初期フォーカス（コンテナまたは見出しが候補だが未実証）
- 操作要素1件のモーダルの初期フォーカス

上記は2アプリの対象範囲に実例がなく、または実測検証を伴わないコードレビューのみの参考情報であるため、最低要件・推奨要件として確定しない。

matching-app・schedule-appの具体的な要素id（例：特定モーダルのキャンセルボタンのid）は、本文書の正式要件としては記載しない。20章「実装パイロット」の実例としてのみ記録する。

---

## 9. Focus Trap

### 9.1 最低要件（M）

1. **M** — Tabキーで、フォーカス可能な最後の要素から最初の要素へ循環する。
2. **M** — Shift+Tabキーで、フォーカス可能な最初の要素から最後の要素へ循環する。
3. **M** — `disabled`状態の要素を候補から除外する。
4. **M** — 非表示の要素（例：`offsetParent`を持たない要素）を候補から除外する。
5. **M** — `tabindex="-1"`が付与された要素の意図的な除外を尊重する。
6. **M** — モーダル内の動的DOM（後から追加・削除される要素）に追随する。
7. **M** — 候補が0件の場合でも例外を発生させず、安全にreturnする。
8. **M** — 候補一覧を固定配列として長期間保持せず、必要なタイミング（例：キー操作のたび）で再取得する。
9. **M** — 実装後、実ブラウザで実際のTab順を確認する。静的なセレクタの確認だけで完了と判定しない。

### 9.2 candidate抽出の参考例（正式仕様ではない）

以下は、matching-app・schedule-appで実際に使われた抽出条件の例である。**具体的なセレクタ文字列を正式仕様として固定するものではなく**、達成すべき条件（フォーカス可能な操作要素をすべて含み、無効化・非表示・意図的除外された要素を含まない）を満たす実装例として参考記載する。

```
button, input, select, textarea, a[href], [tabindex]:not([tabindex="-1"])
```

**ただし、上記のようなセレクタだけで、実際のブラウザのTab順を完全に再現できるとは限らない。** 9.3節を参照。

### 9.3 スクロール可能領域の暗黙的フォーカス（重要な既知事項）

schedule-app.htmlの印刷プレビュー領域（`#print-preview-area`）は、`tabindex`属性を一切持たないにもかかわらず、`overflow-y:auto`のスクロール可能領域であることを理由に、ブラウザネイティブの暗黙的Tab到達性を獲得する。この種の要素は、9.2節のようなセレクタベースのFocus Trap候補配列には含まれない。

このため、以下を一般原則として記載する。

- スクロール可能領域等が、明示的な`tabindex`なしにブラウザのTab順へ入る場合がある **（M — この可能性を実装前に認識すること）**
- 候補配列の外にあるactiveElement（例：スクロール可能領域にフォーカスがある状態）から、次にTab／Shift+Tabしてもモーダル外へフォーカスが流出しないことを、実ブラウザで実測する **（M）**
- 静的なセレクタ確認だけでFocus Trapの検証を完了したと判定しない **（M）**

schedule-appでの実測（15回連続Tab・15回連続Shift+Tabの繰り返し、スクロール後のTab）では、候補配列とブラウザの実Tab順が完全一致しないにもかかわらず、モーダル外への流出は発生しなかった。ただしこれは1実装例の実測結果であり、他アプリへの一般的な保証ではない。

### 9.4 未決定（U）

- 候補が1件のみのモーダルの正式なFocus Trap要件——matching-app.htmlの`how-ov`（focusable候補は`btn-close-how`の1件のみ。見出しは`tabindex="-1"`のため候補外）で、Tab・Shift+Tabとも連続操作しても同一候補に留まり背景へ流出しないことを実測済みである。ただし1アプリ1モーダルでの実証にとどまるため、共通要件としてのM／R確定は次Phase以降の追加実証を待ち、現時点では**U**のまま維持する。
- 複数モーダルが同時に開いている場合のFocus Trapの正式要件
- ネイティブ`<dialog>`を使用する場合のFocus Trapの扱い（4アプリとも未使用のため未検証）

### 9.5 Switch Scan動作中の扱い

Switch Scanがモーダルと同時に動作しうるアプリでは、Focus TrapとSwitch Scanが同時に競合しないことを確認する（詳細は11章）。

---

## 10. Escape

Escapeによるモーダル閉鎖は、**全モーダル一律の最低要件にはしない。** 実装するかどうかはモーダルの性質によって判断し、実装する場合の優先順位ルールのみを最低要件とする。

1. **R** — 通常モーダル（説明中心・設定・破壊的確認等）では、Escapeによる閉鎖を推奨する。
2. **M（Escapeを実装する場合）** — 共通A11yパネルが開いている場合は、モーダルのEscape処理より共通A11yパネルを優先して閉じる。
3. **C（PINロックの場合）** — PINロックはDesign System 7.6.3節を正本とし、Escapeによるバイパスを認めない（意図的仕様）。本文書はこの相違点の記録にとどめ、要件そのものはDesign System側が正本である。
4. **C（外部ダイアログ表示中）** — 印刷ダイアログやOSファイル選択ダイアログが開いている間のEscapeは、ブラウザ／OS側の挙動へ委ねる（17章参照）。
5. **C（破壊的確認の場合）** — Escapeをキャンセル操作と同義にできるかどうかは、モーダルの意味に応じて個別に確認する。
6. **U** — 保存処理中のモーダルにおけるEscapeの扱い（2アプリに実証例なし）
7. **U** — 称賛・完了演出モーダルにおけるEscapeの扱い（19章参照）
8. **U** — 複数モーダルが同時に開いている場合のEscapeの扱い
9. **A** — Escapeをグローバルハンドラ（アプリ全体で1つ）で処理するか、モーダル個別のハンドラで処理するかは、アプリの既存実装構造に依存するため、アプリ固有の選択とする。

---

## 11. 背景操作抑制

「モーダル表示中、背景の実操作（クリック・フォーカス）を防ぐ」という**目的そのものはM（最低要件）**とする。ただし、**実現する具体的な実装方式はC（条件付き）またはA（アプリ固有）**とする。

### 11.1 目的（M）

- **M** — モーダル表示中、背景側の要素へ実ユーザーの操作（マウスクリック・キーボードフォーカス）が到達しないようにする。

### 11.2 実装方式（C／A）

以下はいずれも実装方式の選択肢であり、特定の方式（例：`inert`属性を背景要素へ付与すること）を唯一の最低要件とはしない。

- **A** — `inert`属性を背景ラッパー要素へ付与する方式（matching-appの実装例）
- **A** — 背景ラッパー全体を操作不能化する方式
- **A** — 全画面オーバーレイ（`position:fixed;inset:0`等）による構造的抑制（schedule-appの実装例）
- **A** — その他のアプリ固有方式

背景ラッパーの構造（`#hdr`/`#main`のような特定の要素分割）はアプリごとに異なるため、**特定アプリの背景要素idを共通要件として記載しない。**

### 11.3 重要な既知事項

- **Focus Trapと背景操作抑制は、別の防御層である。** Focus Trapはモーダル内部のキーボード操作を閉じ込める層、背景操作抑制はモーダル外部（背景）への操作到達を防ぐ層であり、一方の実装が他方を代替するとは限らない。
- **全画面オーバーレイだけでは、背景要素へのTab到達や、JavaScriptによるprogrammatic focusを防げない場合がある。** schedule-appの実装では、実マウスクリックについては全画面オーバーレイ構造で抑制されることを実測確認したが、背景Tab到達・programmatic focusまでは防げていない。
- **`inert`属性は、実ユーザーのフォーカス操作・ポインター操作を抑制するが、JavaScriptから直接呼び出される`element.click()`のようなprogrammatic clickは防がない。** この非対称性を実装前に認識する。

### 11.4 未決定・アプリ固有（U／A）

- 共通A11y領域（共通A11yボタン＋共通A11yパネル）については、Keyboard操作可否・固定UI/背景への非流出が16.1節M1〜M7で結果要件として確定済みである。背景操作抑制（本章）からの具体的な除外方法は16.2節（A／C）を参照し、本節では未決定（U）として扱わない。
- 共通A11y領域以外の、その他の共通固定UI（Lock・Home・全画面等）を背景操作抑制の対象から除外すべきかどうか（**U** — 21章6番参照）
- 閉鎖中のモーダル自身へ`inert`を付与するかどうか（**A** — register-app・time-timerは付与、matching-app・schedule-appは付与しない実装差がある）
- `inert`と`aria-hidden`を併用するかどうか（**A** — register-app・time-timerは併用、matching-appは`inert`のみで`aria-hidden`は併用していない実装差がある。機能的な不具合ではないが、スタイルの不一致として記録する）
- 複数モーダルが同時に開いている場合の背景操作抑制の解除条件（**U**）

---

## 12. フォーカス復帰

フォーカス復帰は **U（未決定）** とする。最低要件・推奨要件のいずれにも確定しない。

### 12.1 現状

- matching-app・schedule-appでは**未実装**。
- time-timerには、固定のトリガー要素（FAB等）へ復帰する実装例がある。
- register-appには、`MutationObserver`ベースのフォーカス復帰実装例（`pmOpenerEl`）があるが、**背景の再描画後にフォーカスが`BODY`要素へ戻ってしまう既知の問題が残っている**（Phase18.1-Cで判明、Phase18.6-Bのコードレビューでも同一箇所の構造が変わっていないことを確認）。

### 12.2 未確立な論点

- トリガー要素がモーダルを閉じた時点で画面上から消えている（再描画等で消滅している）場合の、安全な復帰先の標準
- 画面（タブ）切替後の復帰基準
- 破壊的操作後の復帰基準
- 印刷後の復帰基準
- ファイル選択後の復帰基準

いずれも、実証済みで不具合のない実装例が現時点でないため、正式な判断基準を今回定めない。

### 12.3 Switch Scanの位置復帰との違い

Switch Scanにおける走査位置（`scanIdx`相当）のリセット・復帰は、フォーカス復帰とは別の概念であり、別の実装が必要になる。両者を混同しない。

---

## 13. Switch Scan

具体的な変数名・関数名・CSSクラス名は、本文書の正式仕様としない。達成すべき結果のみを原則として記載する。

### 13.1 最低要件（M）

1. **M** — モーダルを実装する前に、そのモーダル表示中もSwitch Scanが動作するアプリかどうかを確認する。
2. **M** — Switch Scanが存在するアプリでは、KeyboardとSwitch Scanが同時に競合しないようにする。

### 13.2 条件付き要件（C）

3. **C**（モーダル中もScanが動作する場合） — 背景側の候補を除外し、モーダル内の候補へ限定する。
4. **C**（Scanがモーダル中は構造的に停止する場合） — 不要な候補限定処理を追加しない（変更不要という判断も正しい実装である）。
5. **C** — Switch ScanがOFF状態のとき、モーダルの開閉によって勝手に再開しないようにする。
6. **C** — 候補集合が変化する場合（動的DOM等）、走査位置・ハイライト表示を安全に再評価する。

### 13.3 未決定（U）

- 候補が1件のみの場合のSwitch Scan固有挙動
- 候補が0件の場合のSwitch Scan固有挙動
- 視線入力との統合（本文書作成時点で視線入力を持つアプリの実測調査を行っていない）
- 全アプリ共通の候補抽出方式の統一

matching-appとschedule-appでSwitch Scanへの対応が根本的に異なる（前者はモーダル中も動作するため候補限定が必要、後者は構造的にモーダルと同時発生しないため変更不要）ことは、20章「実装パイロット」に記録する。

---

## 14. 動的DOM

1. **M** — 候補（Focus TrapやSwitch Scanが参照する要素一覧）を固定配列として長期間保持しない。
2. **M** — DOM要素の追加・削除後、候補を再取得する。
3. **M** — 要素の`disabled`・非表示状態の変化に追随する。
4. **R** — Focus TrapとSwitch Scanで、必要な候補の集合が異なりうることを認識する（一方の候補一覧をもう一方へ流用しない）。
5. **M** — スクロール可能領域等、明示的な`tabindex`なしにブラウザが暗黙的にフォーカス可能とする要素があることに注意する（9.3節参照）。

**`MutationObserver`の使用は必須にしない。** イベント（keydown等）ごとの再取得、`MutationObserver`の使用、その他の具体的な実装方式は、いずれも **A（アプリ固有）** とする。matching-app・schedule-appのFocus Trapはいずれもイベントごとの再取得方式のみで実現しており、`MutationObserver`を用いていない。

---

## 15. 外部ダイアログ

以下は、アプリ側JavaScriptの完全な制御対象外である。

- `window.print()`が開く印刷ダイアログ
- OSファイル選択ダイアログ
- カメラ入力の許可・選択UI
- ブラウザの権限確認ダイアログ

### 15.1 原則

1. **M** — Escapeの挙動は外部ダイアログ側（ブラウザ／OS）へ委ねる。アプリ側で制御しようとしない。
2. **M** — 外部ダイアログを開いた呼び出し元のモーダルが、想定どおりの状態（開いたまま、または適切に閉じている）を維持していることを確認する。
3. **U** — 外部ダイアログを閉じた後のフォーカス復帰は未決定（12章参照）。
4. **M** — 自動テスト（CDP等）で外部ダイアログを実際に操作できない場合、到達性の確認（キーボードでその起動ボタンまで到達できるか）とコードレビューまでにとどめ、**「未実施」であることを明記する。**
5. **R** — 可能であれば、実機・実ブラウザでの手動確認を行う。

**自動テストで確認していない項目を「確認済み」と記載してはならない。** これは本文書全体、および本文書に基づく実装検証報告に共通する原則とする。

---

## 16. 共通A11yパネル

共通A11yパネル（`donomanaA11yPanel`）自体のARIA構造・実装は参考章として扱う。**本仕様書の要件をそのまま適用しない。**

- `donomanaA11yPanel`は`role="dialog"`を持つが、`aria-modal`属性は持たない。そのため6.2節のとおり本文書の主対象外である。
- モーダルと共通A11yパネルが同時に開く可能性がある。
- Escapeを実装する場合は、共通A11yパネルを優先して閉じる（10章M2参照。本節では重複記載しない）。
- 共通A11yパネル自体の実装は`generate.js`のテンプレート関数が管轄する（CLAUDE.md参照）。個別アプリファイルを直接編集する対象ではない。

### 16.1 モーダル側Focus Trapとの関係（M）

以下は、共通A11yパネル自体の要件ではなく、**モーダル側のFocus Trapが共通A11yパネルとどう関係すべきか**という結果要件である。matching-app.html・schedule-app.htmlの2アプリ・異なる実装方式（20.1節・20.2節参照）で実証済みのため、v1.1でMへ確定する。

1. **M** — モーダル表示中でも、共通A11yボタンおよび共通A11yパネルへKeyboardで到達できること。
2. **M** — 共通A11yパネル内部の、表示中・有効な操作要素をKeyboardで操作できること。
3. **M** — 共通A11yパネル操作中に、Lock・Home・全画面等の**他の**共通固定UIへフォーカスが流出しないこと。
4. **M** — 共通A11yパネル操作中に、背景コンテンツへフォーカスが流出しないこと。
5. **M** — 共通A11y領域（共通A11yボタン＋パネル内部）の端点からTab／Shift+Tabした際に、他の固定UI・BODY・背景・モーダル要素へ一時的にも着地する「ワンホップ」を発生させないこと。
6. **M** — 共通A11yパネルを閉じた後、次のTab／Shift+Tabでモーダル内へ再捕捉されること。即時の`focus()`によるフォーカス復帰処理は必須としない（12章参照）。
7. **M** — 共通固定UI全体をFocus Trapの例外として扱わず、例外の対象は共通A11y領域（共通A11yボタン＋パネル）に限定すること。

### 16.2 実装方式（A／C）

上記M1〜M7は結果要件であり、具体的な実現方法はアプリのHTML構造に依存する。

- **A** — 共通A11yボタンをFocus Trapの候補配列へ含めるかどうか。
- **A** — 共通A11yパネル内部に独自の端点循環（M5達成のための仕組み）を実装するかどうか。
- **A** — 候補配列の具体的な変数名・セレクタ構成。
- **A** — Switch Scan動作中にFocus Trap全体を無効化するかどうか（13章参照）。
- **C** — 背景操作抑制（11章。例：`inert`）等の外部防御層が存在しない場合は、共通A11y領域内部に端点循環、またはワンホップを防止できる同等の仕組みを設けること。外部防御層が存在する場合は、共通A11y領域への不介入（Trapが単に`return`する実装）だけで足りることがある。

具体的な実装パターンは20.1節（matching-app方式）・20.2節（schedule-app方式）を参照する。いずれも唯一の正式実装方式ではなく、アプリの既存防御層に応じて選択される実装例である。

---

## 17. 称賛・完了演出

本章は **未決定章** であり、現状の記録にとどめる。正式要件は決定しない。

### 17.1 現状（matching-app）

- `clear-ov`（クリア演出）・`vs-result-ov`（対戦結果表示）の2モーダルが該当する。
- 通常モーダル基準の一部（`role="dialog"`・`aria-modal="true"`）は既に満たしている。
- `aria-labelledby`・Focus Trapは**未整備**（Phase18.1〜18.3の対象外だったため）。
- ゲーム進行のロジックと密結合しており、一般化可能性は低い。
- 初期フォーカスは、既存の「もういちど」系ボタンへ向ける実装が既にある。
- Escapeは、モーダル種別を問わない既存の共通グローバルハンドラで閉じる対象に含まれている。
- 背景操作抑制は、他の3モーダル（how-ov/settings-ov/edit-ov）と異なり**未検証**。

### 17.2 Typographyとの関係

称賛・完了演出モーダルの見出しに関する意味分類・階層は、`donomana-typography-spec-v3_0.md` 16.15節・16.16節を参照する。本文書はこれを複製しない。

### 17.3 未決定事項

- 通常モーダル基準（7章〜11章）をどこまで適用すべきか
- `role="alertdialog"`が適切かどうか
- 自動閉鎖（タイマーによる閉鎖）を認めるかどうか
- 背景操作抑制の要否

---

## 18. 検証手順

### 18.1 必須検証

- 静的コード確認
- 通常幅（1280px相当）
- 375×667
- Tab
- Shift+Tab
- 全ての閉じる経路の確認
- Switch Scan有無の事前確認
- Console Error 0
- pageerror 0
- commit・merge・push後の公開版再検証

### 18.2 実装した場合に必須の検証

- 初期フォーカスを実装した場合：初期フォーカス先の実測確認
- Escapeを実装した場合：Escape動作の実測確認
- 背景操作抑制を実装した場合：その実装方式に応じた実測確認

### 18.3 条件付き検証

- 背景へのprogrammatic focus（該当する実装がある場合）
- 背景への実マウスクリック（該当する実装がある場合）
- 動的DOMへの追随（該当する要素がある場合）
- High Contrast表示との相互作用（共通A11yパネルと関係する場合）
- 文字サイズ変更との相互作用（同上）
- 印刷（安全に自動化できる範囲のみ。できない場合は到達性確認とコードレビューにとどめる）
- ファイル選択（同上）
- カメラ（同上）
- その他の外部UI（同上）
- 実機タッチでの確認
- 視線入力での確認（該当アプリのみ）

**確認できなかった項目は「未確認」と明記する。** 確認したかのように記載しない。

---

## 19. 横展開ルール

1. 全アプリへの一括適用を禁止する。
2. 対象アプリごとに、実装前の事前調査Phaseを設ける。
3. Keyboard→背景操作抑制→Switch Scanという段階を分離し、1Phaseで一度にすべてを実装しない。
4. 対象アプリの状況に応じて、必要な段階だけを実施する（例：Switch Scanが構造的にモーダルと同時発生しないアプリでは、Switch Scan関連の変更は不要）。
5. 実装前に、対象モーダルの既存open/close経路をすべて洗い出す。
6. モーダルを閉じる全経路（ボタン・背景クリック・Escape・処理完了後の自動遷移等）を確認する。
7. Switch Scanとの同時動作有無を確認する（13章参照）。
8. 背景ラッパー構造を確認する（11章参照）。
9. 動的DOMの有無を確認する（14章参照）。
10. 印刷・ファイル選択等の外部UIの有無を確認する（15章参照）。
11. commitする前に、未コミット状態で実装・検証を完了させる。
12. Fast-Forward mergeのみを用いる（merge commitを作らない）。
13. push後、公開版で再検証する。
14. **「このアプリには変更が不要」という判断も、成功した調査結果として扱う。**
15. モーダルアクセシビリティ改善と、既存機能の変更を同じPhaseで混在させない。
16. Typographyの変更を同じPhaseに混在させない。

---

## 20. 実装パイロット

具体的なコードは長く転載せず、成功原則とアプリ固有の差異を記録する。

### 20.1 matching-app.html

- 対象：`how-ov`（使い方）／`settings-ov`（設定）／`edit-ov`（セット編集）
- Keyboard（ARIA構造・初期フォーカス・Focus Trap）を実装
- 背景inert（`#hdr`/`#main`への`inert`付与）を実装
- Switch Scan候補限定（モーダル表示中は候補をモーダル内へ限定）を実装
- 動的に増減するカード編集UIへFocus Trap・Switch Scanが追随することを確認
- Switch ScanがOFFの状態から誤ってONにしてしまう自己停止系のテスト誤りを、原因を切り分けたうえで訂正し、実際には安全であることを確認した実例あり
- 背景へのprogrammatic clickは`inert`だけでは制限されないことを確認（11.3節参照）
- 共通A11yパネルとの関係は「A11y領域不介入型」（仮称）：共通A11yパネルが表示中かつactiveElementが共通A11yボタンまたはパネル内部にある場合、モーダルのFocus Trapは介入せず`return`するのみで、パネル領域内部の専用端点循環は実装していない（実装パターンの1つとして**R**）
- この方式が安全に機能する前提は、共通A11y領域から外へ抜けた場合にも背景・固定UIへワンホップしないことを担保する同等の外部防御（本アプリでは背景inert）が別途存在すること（背景防御を伴う場合の条件として**C**）
- 候補が1件のみのモーダル（`how-ov`）でのFocus Trapは、本アプリで実証済み（9.4節参照）
- 共通A11yパネルを閉じた直後は、即時`focus()`による復帰処理を追加せず、次のTab／Shift+Tabで通常のモーダルFocus Trapへ再捕捉される設計とした
- Escapeは10章M2のとおり共通A11yパネルを優先して閉じる

### 20.2 schedule-app.html

- 対象：`new-modal`（新規作成確認）／`print-modal`（印刷設定）／`img-modal`（画像選択）
- Keyboardのみを実装（背景inert・Switch Scan変更は対象外と判断）
- 破壊的確認（`new-modal`）は安全側操作（キャンセル）へ初期フォーカスする設計を採用
- 動的に生成される絵文字グリッド（80個超）へFocus Trapが追随することを確認
- `#print-preview-area`のスクロール可能領域による暗黙的Tab到達を発見（9.3節参照）
- Switch Scanは、モーダルが開くeditorタブでは構造的に停止しているため、候補限定等の変更は不要と判断し、実際に変更しなかった（19章「変更不要判断も成功」の実例）
- 印刷・ファイル選択は外部UIとして扱い、到達性確認とコードレビューにとどめた（15章参照）
- 共通A11yパネルとの関係は「A11y領域端点循環型」（仮称）：共通A11yボタンを候補配列の先頭に含め、共通A11yパネル内部の表示中・有効なfocusable候補を都度取得し、その領域内でも境界循環（候補配列の末尾からTabで先頭へ、先頭からShift+Tabで末尾へ）を行う（実装パターンの1つとして**R**）
- 背景inertは未実装のため、この端点循環が共通固定UI・背景へのワンホップを防止する実質的な唯一の防御層になっている（背景防御がない場合の条件として**C**、16.2節参照）
- Phase18.10-Bにて、共通A11y領域に不介入のみとする実装（matching-app方式と同型）を試験実装したところ、パネル末尾の候補からTabした際に共通固定UI（Lockボタン）へ、パネル先頭の共通A11yボタンからShift+Tabした際にBODYへ、それぞれ一時的に着地する「ワンホップ」を検出した
- Phase18.10-B2にて、共通A11y領域専用の端点循環を追加実装し、上記ワンホップが解消することを実測確認した
- Phase18.10-Cにて、上記解消結果をFast-Forward merge・push後の公開版でも再確認した
- 共通A11yパネルを閉じた直後は、即時`focus()`による復帰処理を追加せず、次のTab／Shift+Tabで通常のモーダルFocus Trapへ再捕捉される設計とした（matching-app方式と同様）
- Escapeは10章M2のとおり共通A11yパネルを優先して閉じる
- `print-modal`のFocus Trap内部候補配列は明示的に4件（セレクタ一致ベース）である。`#print-preview-area`は`tabindex`を持たないスクロール可能`div`であり候補配列には含まれないが、ブラウザの実Tab操作では暗黙的な中間停止点として5段階目に到達する（9.3節記載の既知事象、Phase18.10-Cで公開版から再確認）。境界循環（first/last）は明示候補4件の範囲で正常に機能しており、候補数を単純に「5件」とは扱わない
- schedule-appには`prefs.switchMode`ベースの独自Switch Scan実装が存在し、`activeTab==='viewer'`の場合に動作する。モーダルを開くボタン自体はタブに依存せず操作可能なため、viewerタブを開いたままモーダルを表示した場合、Switch Scan用keydownリスナーとFocus Trap用keydownリスナーが並存しうる。Phase18.10-Cにて`switchMode='2switch'`状態でモーダル内Tabおよび共通A11y領域端点循環を実測し、本Phaseの変更による新規の悪化がないことを確認した。Switch Scanそのものの設計は変更していない

### 20.3 time-timer.html・register-app.html（参考実装）

- いずれも本文書のPhase18パイロット（matching-app・schedule-app）以前から`role="dialog" aria-modal="true"`を含むモーダルを実装済み。
- PINロック（time-timerの解除／PIN設定／PIN削除／ロック実行の4モーダル）は、Design System 7.6.3節を正本とする正式例外であり、本文書の対象外（6.2節参照）。
- register-appには、複数モーダルを共通ヘルパー（`setModalInert`相当）で一元管理する実装例があるが、フォーカス復帰に既知の問題が残っている（12章参照）。

### 20.4 okane-app.html（部分実装・今後の改善候補）

- 対象：4件の既存非PINモーダル（`customModalOverlay`／`helpModalOverlay`／`settingsModalOverlay`／`recordsModalOverlay`）
- ARIA・初期フォーカス・Focus Trap・共通A11yパネル優先のEscapeガードの改善実装が既に存在する
- ただし、matching-app・schedule-appで実証した共通A11yパネル対応Focus Trap（16.1節M1〜M7）は未適用
- 背景inertは未実装
- schedule-appのような`prefs.switchMode`相当のSwitch Scan独自実装は確認されていない
- 本文書のM要件実証根拠（4章・22章の「2アプリでの実証」）には含めない。今後の改善Phase候補として記録する

---

## 21. 未決定事項

以下は、本文書v1.1の時点で未決定である。将来Phaseでの追加実証・追加調査を経て、改訂版で更新する前提とする。

v1.0時点のU項目のうち、次の2件はv1.1で解消し、該当箇所へ移動した。

- ~~モーダル表示中に共通A11yパネルを操作可能にするかどうか~~ → matching-app・schedule-appの2アプリで実証済みのため**M**へ移行（16.1節M1・M2）
- ~~共通A11yパネル操作中の固定UI・背景への非流出~~ → 同じく実証済みのため**M**へ移行（16.1節M3・M4・M5）

引き続きUとする項目：

1. フォーカス復帰（最低要件・推奨要件のいずれにするか）
2. Focus Trap候補が1件のみの場合の共通処理（1アプリ1モーダルでの実証にとどまるため。9.4節参照）
3. 称賛・完了演出モーダルの正式要件
4. `role="alertdialog"`の使用基準
5. ネイティブ`<dialog>`要素への移行方針
6. **共通A11y領域（共通A11yボタン＋共通A11yパネル）以外の、その他の共通固定UI（Lock・Home・全画面等）を、11章の背景操作抑制（`inert`等）の対象から除外すべきかどうか**（v1.0の「共通A11yボタン等を背景操作抑制の対象から除外するかどうか」という曖昧な表現を、共通A11y領域とその他の共通固定UIとに分けて整理し直したもの。共通A11y領域についてはFocus Trap上の扱いが16.1節M7でM化されたが、11章の背景操作抑制そのものの対象範囲は未決定のまま）
7. 複数モーダルが同時に開いている場合の正式な取り扱い（Focus Trap・Escapeとも）
8. 閉鎖中のモーダル自身へ`inert`を付与するかどうかの統一方針
9. `inert`と`aria-hidden`を併用するかどうかの統一方針
10. 視線入力固有の要件
11. PIN以外の用途で新規例外モーダルが必要になった場合の、Design Systemへの個別記録の詳細手続き
12. 全アプリ共通のSwitch Scan候補抽出方式の統一（現時点ではアプリ固有のまま）
13. すべてのアプリでFocus Trap実装方式（A11y領域不介入型／A11y領域端点循環型）を統一するか、アプリ固有のまま維持するか
14. 候補1件モーダルのFocus Trap要件を共通のM／Rへ確定するタイミング（追加実証アプリ数の目安を含む）
15. 未改善アプリ（okane-app等）への適用順序

---

## 22. 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| v1.0 | 2026-08-01 | 初版。**未承認ドラフト**。Phase18.1〜18.7の調査・実装・検証結果を反映。matching-app・schedule-appの2アプリでの実証結果を最低要件（M）の根拠とした。Design System `donomana-design-system-v2_0.html` 9.9.1 #8（新規採用制限と既存改善の分離）と整合させた。フォーカス復帰・称賛完了演出・候補1件時の扱い等、根拠が不十分な事項は未決定（U）のまま残した。 |
| v1.1 | 2026-08-02 | Phase18.9〜18.10（matching-app：Phase18.9-B／B2／C、schedule-app：Phase18.10-B／B2／C）の実証結果を反映。共通A11yパネル操作可否・固定UI/背景への非流出・ワンホップ非発生・パネル閉鎖後再捕捉を**U→M**へ移行し、16.1節へ集約（結果要件はM、実装方式はA／Cとして16.2節で分離）。matching-app（A11y領域不介入型、背景inert前提）・schedule-app（A11y領域端点循環型、背景inertなしでの条件付き要件）の2実装パターンを20.1節・20.2節へ記録。9.4節の「候補1件モーダルに該当実例なし」という事実誤認を、matching-appの`how-ov`実証結果に基づき訂正（分類は**U**のまま維持）。20.2節にschedule-appの`prefs.switchMode`（Switch Scan）とFocus Trapの並存、および`#print-preview-area`の候補数表現（明示候補4件／実Tab5段階）を補足。4章・20.4節にokane-app.htmlの部分実装状況を、実証対象とは区別して限定的に記録。21章の未決定事項を整理し、解消済み2項目をM要件へ移行、残る項目は「共通A11y領域」と「その他の共通固定UI」の区別を明確化。承認状態は引き続き**未承認ドラフト**。 |
| v1.1 | 2026-08-02 | v1.1を**承認済み**とした。レビュー完了・main統合済みの状態へ移行し、既存モーダル改善の共通基準として運用を開始する。本文のM/R/C/U/A分類・実装パイロット・未決定事項は変更しない。dev-rules 17章の参照説明も合わせて更新した。 |

---

*本文書v1.1は2026-08-02に承認済みである。既存モーダル改善の共通基準として運用し、未決定（U）事項は継続管理する。A／C項目はアプリ構造に応じて適用する。*
